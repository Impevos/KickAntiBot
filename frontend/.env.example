# Geliştirme: boş bırakın — Vite proxy /api isteklerini localhost:3000'e yönlendirir.
# Production build için tam URL girin (ör. https://api.example.com).
VITE_API_BASE_URL=

# true = backend olmadan mock veri; false = gerçek API
VITE_USE_MOCK_DATA=false
